# Segredos: nunca versionar
.env

# Sistema
*.log
.DS_Store

# Token de leitura do cluster k3s
prometheus/k3s-token

# Metricas geradas em runtime pelo CronJob de backup
textfile/finance-backup.prom

# Metrica gerada em runtime pelo cron do host
textfile/openclaw.prom

# Backups manuais de edicao, nao versionar
.bak-archive/
*.bak-*
